div.top
	div.header
		div.title
			h1 Twicas Cruise
			p ランダムでキャスを旋回できるサービス
		div.button(ng-click="login()")
			p START CRUISE
	div.main(ng-repeat="card in cards")
		div.card
			img(src="{{live.thumbnail}}")
			div.caps
				h2 {{live.title}}
				p {{live.subtitle}}
				p {{live.name}}